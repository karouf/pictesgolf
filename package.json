{
  "scripts": {
    "install": "cd frontend && npm install && cd ..",
    "postinstall": "cd frontend && ember install && cd .. && ./build.sh"
  },
  "dependencies": {
    "ember-cli": "0.2.1",
    "ember-cli-htmlbars": "0.7.6"
  }
}
