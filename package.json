{
  "scripts": {
    "install": "cd frontend && npm install --production && bower install && cd ..",
    "postinstall": "./build.sh"
  },
  "dependencies": {
    "ember-cli": "0.2.1",
    "bower": "^1.3.12"
  }
}
