{
  "scripts": {
    "install": "cd frontend && npm install --production && cd ..",
    "postinstall": "cd frontend && ember install && bower install && cd .. && ./build.sh"
  },
  "dependencies": {
    "ember-cli": "0.2.1"
  }
}
